import React from 'react';
import './loading.css';

const Loading = () => {
    return (
        <div>
            <div className="loader">
                <h1>Loading...</h1>
            </div>
        </div>
    );
};

export default Loading;